<nav class="navbar-default navbar-static-side" role="navigation">
    <div class="sidebar-collapse">
        <ul class="nav" id="side-menu">
            <li class="nav-header">
                <div class="dropdown profile-element">
                    <span>
                        <img alt="image" class="img-circle img-responsive" src="~/Images/avatar_white.png" style="width:50px" />
                    </span>
                    <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                        <h3 class="m-b-xs">
                            <strong class="font-bold">@User.Identity.Name<b class="caret"></b></strong>
                        </h3>
                        @*<span class="text-muted text-xs block"><b class="caret"></b></span>*@
                    </a>
                    <ul class="dropdown-menu animated fadeInRight m-t-xs">
                        <li><a href="@Url.Action("ChangePassword", "Manage")">Change Password</a></li>
                        <li class="divider"></li>
                        @if (User.IsInRole("Admin"))
                        {
                            <li><a href="@Url.Action("CreateUser", "Account")">Create User</a></li>
                            <li class="divider"></li>
                        }
                        <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
                    </ul>
                </div>
                <div class="logo-element">
                    <img src="~/Images/uni-credit.png" style="width: 42px; " />
                </div>
            </li>
            @if (User.IsInRole("Admin"))
            {
                <li class="@Html.IsSelected(controller: " CommiteeProgrammes")">
                    <a href="@Url.Action("Index", "CommiteeProgrammes")"><i class="fa fa-calendar-check-o"></i> <span class="nav-label">Commitee Programs</span></a>
                </li>
                @*<li class="@Html.IsSelected(controller: " Account")">
                        <a href="@Url.Action("CreateUser", "Account")"><i class="fa fa-user-circle"></i> <span class="nav-label">Create New User</span></a>
                    </li>*@
            }

            @*<li class="special_link @Html.IsSelected(controller: " CommiteeItems")">*@
            <li class="@Html.IsSelected(controller: " CommiteeItems")">
                <a href="@Url.Action("Index", "CommiteeItems")"><i class="fa fa-list-ol"></i> <span class="nav-label">Commitee Items</span></a>
            </li>
        </ul>
    </div>
</nav>